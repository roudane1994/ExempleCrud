{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} - Creation d'une Categorie{% endblock %}

{% block body %}
<div class="container">

<legend class="scheduler-border">Formulaire de Création d'une Categorie :</legend>
	 {{ form_start(form, {'attr': {'class': 'form-horizontal'}}) }}
         
         
		<div class="form-group">
		{{ form_label(form.name, "Name", {'label_attr': {'class': 'col-sm-2 control-label'}}) }}
		 <div class="col-sm-10">
                {{ form_widget(form.name, {'attr': {'class': 'form-control'}}) }}
                  </div>
                <p>{{ form_errors(form.name) }}</p>
		</div>
		
		<div class="form-group row">
		<div class="offset-sm-2 col-sm-7 pull-right">
		<button type="submit" class="btn btn-primary">Enregister</button>
		</div>
		</div>
	      {{ form_rest(form) }}
               {{ form_end(form) }}
                 </div>
   {% endblock %}
